export default function ComplexDashboard() {
  return (
    <>
      <h4>This is Parallel Route: Complex Dashboard</h4>
      <p>
        Handling error in parallel route when specific route is changed and the
        page is reloaded this will help to display default page instead showing
        the error. by creating default page, in other component which will not
        have navigate in one of parallel route.
      </p>
    </>
  );
}
